- Prefers the assistant to thoroughly analyze existing codebase patterns (components, styling, dependencies) before implementing new features or fixes. Confidence: 0.95
- When fixing bugs, wants changes scoped to functionality only — the existing layout, design, and visible markup must not be disturbed (explicitly asked to keep the UI and settings wiring untouched while replacing the underlying logic). Confidence: 0.9
- Prefers leveraging existing npm packages/dependencies already in the project over building custom implementations from scratch. Confidence: 0.75
- Values visual and structural consistency across dashboard/components — new components should match the styling patterns of existing ones. Confidence: 0.7
- Does not want the assistant to push to GitHub or perform remote git operations while actively working on the code themselves. Prefers to retain control over when code reaches GitHub. Confidence: 0.7
- When given a design mockup or reference image, wants the implementation to replicate it exactly (pixel-faithful visual fidelity) — has been dissatisfied with prior AI outputs that only loosely resembled the design. Confidence: 0.85
- Prefers pragmatic pivots over prolonged debugging: when a feature is blocked by an environment/browser-specific issue (e.g., a Chrome quirk), would rather rework the feature into an alternative implementation than keep trying fixes for the same problem. Confidence: 0.6
- Rejects hacky workaround implementations (e.g., sandboxed iframe/postMessage bridges) in components; explicitly wants a fresh, clean, direct approach instead of reusing workaround methods. Confidence: 0.8
- Prefers the simplest working implementation over retaining feature richness: when presented options (plain YouTube iframe vs. IFrame API via sandbox vs. direct audio), chose the plain iframe, accepting the loss of custom play/pause/seek controls in exchange for a strict-CSP-compliant, clean solution. Confidence: 0.8
- Wants rejected/experimental code removed from the codebase completely — not just left unused or untracked; when a workaround approach is abandoned, any leftover experiment files (e.g., sandbox bridge html) should be deleted and verified gone. Confidence: 0.7
- Dislikes speculative/unrelated changes to configuration files (e.g., `manifest.json` permissions, host permissions, DNR rulesets) made while debugging a symptom; when such files are touched, expects them restored exactly to their original state, keeping only the changes that are part of the actual fix. Confidence: 0.85
